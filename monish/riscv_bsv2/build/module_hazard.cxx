/*
 * Generated by Bluespec Compiler, version 2025.01.1 (build 65e3a87a)
 * 
 * On Sat Jul 11 22:21:05 UTC 2026
 * 
 */
#include "bluesim_primitives.h"
#include "module_hazard.h"


/* Constructor */
MOD_module_hazard::MOD_module_hazard(tSimStateHdl simHdl, char const *name, Module *parent)
  : Module(simHdl, name, parent)
{
  PORT_hazard_exMemRead = false;
  PORT_hazard_exRd = 0u;
  PORT_hazard_rs1 = 0u;
  PORT_hazard_rs2 = 0u;
  PORT_hazard = 0u;
  PORT_RDY_hazard = false;
  symbol_count = 7u;
  symbols = new tSym[symbol_count];
  init_symbols_0();
}


/* Symbol init fns */

void MOD_module_hazard::init_symbols_0()
{
  init_symbol(&symbols[0u], "CAN_FIRE_hazard", SYM_DEF, &DEF_CAN_FIRE_hazard, 1u);
  init_symbol(&symbols[1u], "hazard", SYM_PORT, &PORT_hazard, 3u);
  init_symbol(&symbols[2u], "hazard_exMemRead", SYM_PORT, &PORT_hazard_exMemRead, 1u);
  init_symbol(&symbols[3u], "hazard_exRd", SYM_PORT, &PORT_hazard_exRd, 5u);
  init_symbol(&symbols[4u], "hazard_rs1", SYM_PORT, &PORT_hazard_rs1, 5u);
  init_symbol(&symbols[5u], "hazard_rs2", SYM_PORT, &PORT_hazard_rs2, 5u);
  init_symbol(&symbols[6u], "RDY_hazard", SYM_PORT, &PORT_RDY_hazard, 1u);
}


/* Rule actions */


/* Methods */

tUInt8 MOD_module_hazard::METH_hazard(tUInt8 ARG_hazard_exMemRead,
				      tUInt8 ARG_hazard_exRd,
				      tUInt8 ARG_hazard_rs1,
				      tUInt8 ARG_hazard_rs2)
{
  tUInt8 DEF_hazard_exMemRead_AND_NOT_hazard_exRd_EQ_0_AND__ETC___d7;
  PORT_hazard_exMemRead = ARG_hazard_exMemRead;
  PORT_hazard_exRd = ARG_hazard_exRd;
  PORT_hazard_rs1 = ARG_hazard_rs1;
  PORT_hazard_rs2 = ARG_hazard_rs2;
  DEF_hazard_exMemRead_AND_NOT_hazard_exRd_EQ_0_AND__ETC___d7 = ARG_hazard_exMemRead && (!(ARG_hazard_exRd == (tUInt8)0u) && (ARG_hazard_exRd == ARG_hazard_rs1 || ARG_hazard_exRd == ARG_hazard_rs2));
  PORT_hazard = (tUInt8)7u & (((DEF_hazard_exMemRead_AND_NOT_hazard_exRd_EQ_0_AND__ETC___d7 << 2u) | (DEF_hazard_exMemRead_AND_NOT_hazard_exRd_EQ_0_AND__ETC___d7 << 1u)) | DEF_hazard_exMemRead_AND_NOT_hazard_exRd_EQ_0_AND__ETC___d7);
  return PORT_hazard;
}

tUInt8 MOD_module_hazard::METH_RDY_hazard()
{
  DEF_CAN_FIRE_hazard = (tUInt8)1u;
  PORT_RDY_hazard = DEF_CAN_FIRE_hazard;
  return PORT_RDY_hazard;
}


/* Reset routines */


/* Static handles to reset routines */


/* Functions for the parent module to register its reset fns */


/* Functions to set the elaborated clock id */


/* State dumping routine */
void MOD_module_hazard::dump_state(unsigned int indent)
{
}


/* VCD dumping routines */

unsigned int MOD_module_hazard::dump_VCD_defs(unsigned int levels)
{
  vcd_write_scope_start(sim_hdl, inst_name);
  vcd_num = vcd_reserve_ids(sim_hdl, 7u);
  unsigned int num = vcd_num;
  for (unsigned int clk = 0u; clk < bk_num_clocks(sim_hdl); ++clk)
    vcd_add_clock_def(sim_hdl, this, bk_clock_name(sim_hdl, clk), bk_clock_vcd_num(sim_hdl, clk));
  vcd_write_def(sim_hdl, num++, "CAN_FIRE_hazard", 1u);
  vcd_write_def(sim_hdl, num++, "RDY_hazard", 1u);
  vcd_write_def(sim_hdl, num++, "hazard", 3u);
  vcd_write_def(sim_hdl, num++, "hazard_exMemRead", 1u);
  vcd_write_def(sim_hdl, num++, "hazard_exRd", 5u);
  vcd_write_def(sim_hdl, num++, "hazard_rs1", 5u);
  vcd_write_def(sim_hdl, num++, "hazard_rs2", 5u);
  vcd_write_scope_end(sim_hdl);
  return num;
}

void MOD_module_hazard::dump_VCD(tVCDDumpType dt, unsigned int levels, MOD_module_hazard &backing)
{
  vcd_defs(dt, backing);
}

void MOD_module_hazard::vcd_defs(tVCDDumpType dt, MOD_module_hazard &backing)
{
  unsigned int num = vcd_num;
  if (dt == VCD_DUMP_XS)
  {
    vcd_write_x(sim_hdl, num++, 1u);
    vcd_write_x(sim_hdl, num++, 1u);
    vcd_write_x(sim_hdl, num++, 3u);
    vcd_write_x(sim_hdl, num++, 1u);
    vcd_write_x(sim_hdl, num++, 5u);
    vcd_write_x(sim_hdl, num++, 5u);
    vcd_write_x(sim_hdl, num++, 5u);
  }
  else
    if (dt == VCD_DUMP_CHANGES)
    {
      if ((backing.DEF_CAN_FIRE_hazard) != DEF_CAN_FIRE_hazard)
      {
	vcd_write_val(sim_hdl, num, DEF_CAN_FIRE_hazard, 1u);
	backing.DEF_CAN_FIRE_hazard = DEF_CAN_FIRE_hazard;
      }
      ++num;
      if ((backing.PORT_RDY_hazard) != PORT_RDY_hazard)
      {
	vcd_write_val(sim_hdl, num, PORT_RDY_hazard, 1u);
	backing.PORT_RDY_hazard = PORT_RDY_hazard;
      }
      ++num;
      if ((backing.PORT_hazard) != PORT_hazard)
      {
	vcd_write_val(sim_hdl, num, PORT_hazard, 3u);
	backing.PORT_hazard = PORT_hazard;
      }
      ++num;
      if ((backing.PORT_hazard_exMemRead) != PORT_hazard_exMemRead)
      {
	vcd_write_val(sim_hdl, num, PORT_hazard_exMemRead, 1u);
	backing.PORT_hazard_exMemRead = PORT_hazard_exMemRead;
      }
      ++num;
      if ((backing.PORT_hazard_exRd) != PORT_hazard_exRd)
      {
	vcd_write_val(sim_hdl, num, PORT_hazard_exRd, 5u);
	backing.PORT_hazard_exRd = PORT_hazard_exRd;
      }
      ++num;
      if ((backing.PORT_hazard_rs1) != PORT_hazard_rs1)
      {
	vcd_write_val(sim_hdl, num, PORT_hazard_rs1, 5u);
	backing.PORT_hazard_rs1 = PORT_hazard_rs1;
      }
      ++num;
      if ((backing.PORT_hazard_rs2) != PORT_hazard_rs2)
      {
	vcd_write_val(sim_hdl, num, PORT_hazard_rs2, 5u);
	backing.PORT_hazard_rs2 = PORT_hazard_rs2;
      }
      ++num;
    }
    else
    {
      vcd_write_val(sim_hdl, num++, DEF_CAN_FIRE_hazard, 1u);
      backing.DEF_CAN_FIRE_hazard = DEF_CAN_FIRE_hazard;
      vcd_write_val(sim_hdl, num++, PORT_RDY_hazard, 1u);
      backing.PORT_RDY_hazard = PORT_RDY_hazard;
      vcd_write_val(sim_hdl, num++, PORT_hazard, 3u);
      backing.PORT_hazard = PORT_hazard;
      vcd_write_val(sim_hdl, num++, PORT_hazard_exMemRead, 1u);
      backing.PORT_hazard_exMemRead = PORT_hazard_exMemRead;
      vcd_write_val(sim_hdl, num++, PORT_hazard_exRd, 5u);
      backing.PORT_hazard_exRd = PORT_hazard_exRd;
      vcd_write_val(sim_hdl, num++, PORT_hazard_rs1, 5u);
      backing.PORT_hazard_rs1 = PORT_hazard_rs1;
      vcd_write_val(sim_hdl, num++, PORT_hazard_rs2, 5u);
      backing.PORT_hazard_rs2 = PORT_hazard_rs2;
    }
}
